SymbolDiscovery — найти все места с [TraceId].

NameFormatting — сделать “стабильное полное имя” (для хэша и для отчётов).

IdComputation — hash/override, единая логика.

CollisionDetector — если коллизии → diagnostic error.

SourceEmitter — собрать итоговые .g.cs.

Diagnostics — централизованные сообщения компилятора.
